services:
 - docker

before_install:
 - docker build https://github.com/stts-se/pronlex.git -t pronlex
 - docker build https://github.com/stts-se/wikispeech_mockup.git -t wikispeech
 - docker build https://github.com/HannaLindgren/marytts.git -t marytts
 # - docker run --name=pronlex -v /tmp/appdir:/appdir -p 8787:8787 -it pronlex setup
 # - docker run --name=pronlex -v /tmp/appdir:/appdir -p 8787:8787 -it pronlex import_for_ws_tests
 # - docker run --name=pronlex -v /tmp/appdir:/appdir -p 8787:8787 -it pronlex 


