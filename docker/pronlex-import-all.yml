## docker-compose configuration for importing standard lexicon data

version: '3'
services:
  pronlex:
    image: "sttsse/pronlex:$RELEASE"
    volumes:
     - ${LEXDATA_DIR}:/appdir
    command: ["/wikispeech/bin/import_all", "/appdir", "${LEXDATA_DIR}"]
